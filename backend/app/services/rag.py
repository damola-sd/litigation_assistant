async def rag_retrieve(query: str) -> list[str]:
    """
    Stub RAG retrieval. Amit will replace this with real embeddings + vector DB lookup.
    Returns a list of relevant Kenya Law document chunks.
    """
    return []
